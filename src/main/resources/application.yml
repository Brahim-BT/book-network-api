spring:
  application:
    name: book-network-api
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
server:
  port: 8081
  servlet:
    context-path: /api/v1
springdoc:
  default-produces-media-type: application/json

